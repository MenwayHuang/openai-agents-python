# 环境与依赖安装

> 本文档面向 Python 新手，用来在不同电脑上稳定搭建本仓库的学习环境。当前仓库是 `openai-agents-python`，依赖管理方式是 `pyproject.toml + uv.lock`，不是传统的 `requirements.txt`。

## 先说结论

- 推荐统一使用 Python 3.12。
- 本机已验证 `uv sync --python 3.12 --all-extras --all-packages --group dev` 可以正常安装完整开发依赖。
- 当前本机 `.venv` 使用的是 Python 3.12.13。
- 仓库声明支持 `Python >= 3.10`，所以 Python 3.11 也能跑；但为了 Mac、Windows、后续 PPT Agent 项目都减少差异，建议你统一用 Python 3.12。
- 后续你的 PPT Agent 项目也建议采用同样方案：`uv + pyproject.toml + uv.lock + Python 3.12`。

## 为什么推荐 Python 3.12

`pyproject.toml` 中写的是：

```toml
requires-python = ">=3.10"
```

这代表 Python 3.10、3.11、3.12、3.13 等理论上都可以使用。不过学习和创业项目最怕“这台电脑能跑，另一台电脑不行”，所以不要每台电脑随缘用不同版本。

推荐 Python 3.12 的原因：

- Python 3.12 是主流稳定版本，生态兼容已经比较成熟。
- 本仓库当前已经在本机用 Python 3.12 完整安装验证过。
- 比 Python 3.11 更新，但没有激进到只追最新版本。
- 后续你的 PPT Agent、FastAPI 后端、Agent Service 都可以统一 Python 3.12，减少环境排查成本。

如果某个依赖临时对 Python 3.12 有问题，再退回 Python 3.11。平时不建议一开始就让不同电脑混用 3.11 和 3.12。

## 这个仓库有没有 requirements.txt

没有。这个仓库的依赖入口主要是：

- `pyproject.toml`：声明项目依赖、可选依赖、开发依赖和工具配置。
- `uv.lock`：锁定具体依赖版本，保证不同电脑安装出来尽量一致。
- `Makefile`：封装常用命令，例如 `make sync`。

完整开发环境使用：

```bash
uv sync --python 3.12 --all-extras --all-packages --group dev
```

这条命令会做几件事：

- 如果本机没有合适的 Python 3.12，`uv` 会尝试使用或下载对应版本。
- 在项目根目录创建或更新 `.venv` 虚拟环境。
- 根据 `pyproject.toml` 和 `uv.lock` 安装运行依赖、可选扩展依赖、工作区包和开发工具。

## Mac 笔记本安装步骤

适用：你的另一台 MacBook、MacBook Air、Mac mini。

### 1. 确认已经安装 uv

```bash
uv --version
```

如果能看到版本号，就说明 `uv` 已经可用。

如果没有安装，可以用官方安装方式安装：

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

安装后重新打开终端，再执行：

```bash
uv --version
```

### 2. 安装 Python 3.12

```bash
uv python install 3.12
```

查看本机可被 `uv` 使用的 Python：

```bash
uv python list
```

### 3. 克隆你的个人仓库

```bash
git clone git@github.com:MenwayHuang/openai-agents-python.git
cd openai-agents-python
```

如果这台 Mac 还没有配置 GitHub SSH key，也可以先用 HTTPS：

```bash
git clone https://github.com/MenwayHuang/openai-agents-python.git
cd openai-agents-python
```

### 4. 创建虚拟环境并安装完整依赖

```bash
uv sync --python 3.12 --all-extras --all-packages --group dev
```

也可以使用仓库自带命令：

```bash
make sync
```

不过第一次搭环境时，我更推荐显式写出 Python 版本：

```bash
uv sync --python 3.12 --all-extras --all-packages --group dev
```

### 5. 验证安装

```bash
uv run python --version
uv run python -c "import agents, openai, pydantic; print('agents ok'); print(openai.__version__); print(pydantic.__version__)"
uv run ruff --version
uv run pytest --version
```

如果这些命令都能正常输出，就说明环境已经可用于学习和开发。

## Windows 主机安装步骤

适用：你的 Windows 台式机或 Windows 笔记本。建议使用 PowerShell。

### 1. 确认已经安装 uv

在 PowerShell 中执行：

```powershell
uv --version
```

如果没有安装，可以使用官方 PowerShell 安装方式：

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

安装后关闭 PowerShell，重新打开，再执行：

```powershell
uv --version
```

### 2. 安装 Python 3.12

```powershell
uv python install 3.12
```

查看本机可被 `uv` 使用的 Python：

```powershell
uv python list
```

### 3. 克隆你的个人仓库

```powershell
git clone git@github.com:MenwayHuang/openai-agents-python.git
cd openai-agents-python
```

如果 Windows 还没有配置 GitHub SSH key，可以先用 HTTPS：

```powershell
git clone https://github.com/MenwayHuang/openai-agents-python.git
cd openai-agents-python
```

### 4. 创建虚拟环境并安装完整依赖

```powershell
uv sync --python 3.12 --all-extras --all-packages --group dev
```

安装完成后，项目根目录会出现 `.venv` 目录。

### 5. 是否需要手动激活虚拟环境

平时不一定需要激活。推荐直接用：

```powershell
uv run python --version
uv run pytest --version
```

如果你想手动激活虚拟环境：

```powershell
.venv\Scripts\Activate.ps1
```

如果 PowerShell 提示脚本执行被禁止，可以执行：

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

然后重新激活：

```powershell
.venv\Scripts\Activate.ps1
```

### 6. 验证安装

```powershell
uv run python --version
uv run python -c "import agents, openai, pydantic; print('agents ok'); print(openai.__version__); print(pydantic.__version__)"
uv run ruff --version
uv run pytest --version
```

如果这些命令都能正常输出，就说明 Windows 环境已经可用。

## 常用命令

安装或刷新依赖：

```bash
uv sync --python 3.12 --all-extras --all-packages --group dev
```

运行 Python：

```bash
uv run python
```

运行某个 Python 文件：

```bash
uv run python examples/basic/hello_world.py
```

运行测试：

```bash
uv run pytest
```

只运行匹配某个关键词的测试：

```bash
uv run pytest -k model_provider
```

运行 Ruff 检查：

```bash
uv run ruff check
```

运行 Ruff 格式化：

```bash
uv run ruff format
```

检查锁文件和环境是否一致：

```bash
uv sync --python 3.12 --all-extras --all-packages --group dev --check
```

注意不要只运行 `uv sync --check`。本仓库完整学习环境安装了 `--all-extras --all-packages --group dev`，如果检查命令不带这些参数，`uv` 会按最小默认环境判断，可能提示要卸载一批可选依赖。

## 重新安装虚拟环境

如果依赖安装乱了，可以删除 `.venv` 后重新安装。

Mac：

```bash
rm -rf .venv
uv sync --python 3.12 --all-extras --all-packages --group dev
```

Windows PowerShell：

```powershell
Remove-Item -Recurse -Force .venv
uv sync --python 3.12 --all-extras --all-packages --group dev
```

## 后续 PPT Agent 项目的环境建议

你的 PPT Agent 项目也建议从一开始就统一环境：

- Python 统一用 3.12。
- 包管理统一用 `uv`。
- 项目根目录保留 `pyproject.toml` 和 `uv.lock`。
- 后端服务使用项目内 `.venv`，不要依赖系统全局 Python。
- 真正部署时也固定 Python 3.12 镜像，例如 Docker 中使用 Python 3.12。

一个更适合你后续项目的最小后端结构可以是：

```text
agent-service/
  pyproject.toml
  uv.lock
  .python-version
  app/
    main.py
    agents/
    providers/
    services/
    repositories/
    schemas/
    config.py
  tests/
```

`.python-version` 可以写：

```text
3.12
```

这样你自己、MacBook、Windows 主机、未来云服务器都会优先按同一个 Python 版本安装。

## 关于 TResponseInputItem 的疑问

你问得很对：`TResponseInputItem` 确实不是一个完全中立的抽象类型，它来自 OpenAI Python SDK：

```python
from openai.types.responses import ResponseInputItemParam

TResponseInputItem = ResponseInputItemParam
```

也就是说，在 `openai-agents-python` 这个仓库里，`Model` 虽然是抽象类，但它的输入参数使用了 OpenAI Responses API 的 item 类型作为 SDK 内部统一格式。

这看起来有点奇怪，但对 OpenAI 官方 SDK 来说是一个合理取舍：

- 这个 SDK 默认服务于 OpenAI Responses API。
- Runner、Session、Handoff、Tracing、Tool Call 都需要一个统一的消息和事件格式。
- Chat Completions、LiteLLM、AnyLLM 等路径会把自己的输入输出转换成这个内部格式。
- 官方文档也把非 OpenAI 模型或混合模型栈放在 provider/adapter 层处理，参考 OpenAI 官方文档：[Agents models and providers](https://developers.openai.com/api/docs/guides/agents/models#providers-and-transport)。

所以这个仓库里的理解方式是：

```text
外部模型 API 格式
  -> provider/model adapter 转换
  -> OpenAI Responses item 作为 SDK 内部中间格式
  -> Runner / Tool / Handoff / Session 统一处理
```

但你的 PPT Agent 项目不建议直接照搬这个设计。

你的项目未来要接 OpenAI、Claude、DeepSeek、通义、火山、智谱，甚至你自己的 Go API 网关。如果你一开始就把 `openai.types.responses.ResponseInputItemParam` 放到最底层抽象里，后面会让非 OpenAI provider 永远围着 OpenAI 类型转。

更推荐你的自研项目这样设计：

```text
PPT Agent 自己的中立领域类型
  AgentMessage
  ToolCall
  ToolResult
  ModelRequest
  ModelResponse

OpenAIProvider
  负责 AgentMessage <-> OpenAI Responses API 转换

ClaudeProvider
  负责 AgentMessage <-> Anthropic Messages API 转换

DeepSeekProvider
  负责 AgentMessage <-> OpenAI-compatible Chat Completions 转换
```

这样底层抽象就不被任何一家模型厂商绑定。以后你接另一个模型的 API，也只需要新增一个 provider adapter，不需要改 Agent Runtime 主流程。

一句话总结：

- 学 OpenAI Agents SDK：接受它用 OpenAI Responses item 作为内部统一格式。
- 做你自己的 PPT Agent：定义自己的中立内部格式，把 OpenAI/Claude/国产模型都放到 adapter 层。
